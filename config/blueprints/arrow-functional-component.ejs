import React from 'react';
import PropTypes from 'prop-types';

const function <%= helpers.capitalize(name) %> =(props, context) => {
  return (
  );
}

<%= helpers.capitalize(name) %>.propTypes = {
};

export default <%= helpers.capitalize(name) %>;